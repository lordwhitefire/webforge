#!/usr/bin/env python3
"""
DocIntelligenceCove Agent — Documentation Department
Auto-generated by WebForge agent generator.

THE BODY: This script IS DocIntelligenceCove. It controls what the AI does.
THE BRAIN: The AI only reasons when DocIntelligenceCove asks it to.

Skill file: skills/documentation/doc-intelligence-cove.md
"""

import sys
import os
import json
from pathlib import Path

WEBFORGE_HOME = Path.home() / "webforge"
sys.path.insert(0, str(WEBFORGE_HOME / "agents"))
sys.path.insert(0, str(WEBFORGE_HOME / "mcp"))

from base import Agent


class DocIntelligenceCove(Agent):
    """Auto-generated agent script for DocIntelligenceCove."""

    name = "DocIntelligenceCove"
    department = "Documentation"
    skill_file = "documentation/doc-intelligence-cove.md"
    reports_to = "Thoth"
    can_route_to = ["Thoth", "Hermes"]

    allowed_actions = ["generate_docs", "answer_question", "route", "respond"]
    forbidden_actions = ["write_code", "create_bugfix_task", "create_feature_task", "run_standup", "research", "run_quality_check", "review_code", "learn"]

    # Per-agent correction rules (Daedalus patches these)
    correction_rules = []

    def execute(self, action, data, context):
        """Execute the action. CODE, not AI."""
        if action == "answer_question":
            return {
                "agent": self.name,
                "action": action,
                "message": f"I am {self.name} ({self.department}). I'm ready to work.",
                "next_step": None,
            }
        elif action == "route":
            return self._route_to(data.get("target", "Hermes"), data.get("message", ""))
        return {
            "agent": self.name,
            "action": action,
            "message": f"I am {self.name}. Action: {action}",
            "next_step": None,
        }


def run(message, context=None):
    """Called when developer talks to DocIntelligenceCove."""
    agent = DocIntelligenceCove()
    return agent.run(message, context)


if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("DocIntelligenceCove Agent — Documentation")
        print("Usage: python doc-intelligence-cove.py <message>")
        sys.exit(1)
    message = " ".join(sys.argv[1:])
    result = run(message)
    print(result.get("message", json.dumps(result, indent=2)))
